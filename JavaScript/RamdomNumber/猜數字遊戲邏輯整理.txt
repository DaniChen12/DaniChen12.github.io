按下確認按鈕=取得按鈕的DOM元素，再DOM元素加上監聽事件
先產生1~100其中的隨機數字
點擊了確認按鈕
1.取得輸入的值，取得輸入的數值與答案做比對
2.猜的邏輯:
假設超數數字範圍或不是數字=>alert
猜得數字與答案一樣就 恭喜贏了
猜的數字不一樣 
假設現在是

1~100
min(1) ~ max(100)

的數字範圍，答案是50，如果我猜比答案還

小於答案&&>min    40=? 40~100之間的數字
min =40 

大於50<100    60=? 1~60之間的數字

max =60